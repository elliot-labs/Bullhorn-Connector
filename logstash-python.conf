input {
  exec {
    command => "/home/elliot/BullhornConnector.py JobOrder webResponses,submissions,sendouts,interviews,placements,address,numOpenings,employmentType,usersAssigned,title,c
lientCorporation"
    interval => 15
    codec => json
    tags => ["JobOrder"]
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
  }
  stdout { codec => rubydebug }
}